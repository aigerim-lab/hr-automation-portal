version: '3.8'

services:
  web:
    build: .
    container_name: app-web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
    networks:
      - app_network

networks:
  app_network:
    driver: bridge